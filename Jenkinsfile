pipeline {    agent any    stages {        stage('Build') {            steps {                // Compila el proyecto Java utilizando Maven                bat 'mvn clean package'            }        }        stage('Test') {            steps {                // Ejecuta pruebas unitarias                bat 'mvn test'            }        }        stage('Docker Build and Run') {            steps {                script {                    echo 'Construyendo y ejecutando el contenedor Docker...'                }                // Construye la imagen Docker                bat 'docker build -t myapp .'                // Elimina cualquier contenedor previo                bat 'docker rm -f myapp || true'                // Ejecuta el contenedor Docker                bat 'docker run -d --name myapp -p 8080:8080 myapp'            }        }        stage('Terraform Init') {            steps {                script {                    echo 'Inicializando Terraform...'                }                // Inicializa Terraform                bat 'terraform init'            }        }        stage('Terraform Apply') {            steps {                script {                    echo 'Aplicando configuración de Terraform...'                }                // Aplica la configuración de Terraform                bat 'terraform apply -auto-approve'            }        }    }}